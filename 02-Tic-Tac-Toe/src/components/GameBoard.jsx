import { useState } from 'react';

export default function GameBoard({ onSelectSqare, gameBoard, isGameOver }) {
  return (
    <ol id="game-board">
      {gameBoard.map((row, rowIndex) => (
        <li key={rowIndex}>
          <ol>
            {row.map((playerSymbol, colIndex) => (
              <li key={colIndex}>
                <button
                  onClick={() => onSelectSqare(rowIndex, colIndex)}
                  disabled={playerSymbol !== null || isGameOver}
                >
                  {playerSymbol}
                </button>
              </li>
            ))}
          </ol>
        </li>
      ))}
    </ol>
  );
}
